import { SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY } from './config.js';
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';

const configured = Boolean(SUPABASE_URL && SUPABASE_PUBLISHABLE_KEY);
const supabase = configured ? createClient(SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY) : null;
const state = { user:null, profile:null, route:'day', demands:[], clients:[], contacts:[], contracts:[], attachments:[], demo:!configured };
const $ = (s,root=document)=>root.querySelector(s);
const $$ = (s,root=document)=>[...root.querySelectorAll(s)];
const labels={new:'Novo',in_progress:'Em andamento',waiting_client:'Aguardando cliente',waiting_internal:'Aguardando área interna',need_follow_up:'Preciso cobrar',completed:'Concluído'};
const icons={day:'⌂',capture:'✦',demands:'▤',clients:'♙',contracts:'▱',contacts:'♧',diary:'▣',documents:'□',dashboard:'▥',export:'⇩',copilot:'✧',settings:'⚙'};
const demoDemands=[
 {id:'1',client_name:'Fernando Bernardo da Silva',external_code:'449267',title:'Boleto / novo código',status:'need_follow_up',priority:'urgent',contact:'Lizane',next_action:'Cobrar hoje',next_action_at:new Date().toISOString()},
 {id:'2',client_name:'Edifício Oscar Niemeyer',external_code:'1.110.685',title:'Aumento de tancagem',status:'waiting_internal',priority:'high',contact:'Conta Sim Corporativo',next_action:'Solicitar retorno',next_action_at:new Date().toISOString()},
 {id:'3',client_name:'Márcio Villela',external_code:'339801',title:'Lista de peças / OS',status:'in_progress',priority:'high',contact:'Pablo',next_action:'Acompanhar OS',next_action_at:new Date().toISOString()},
 {id:'4',client_name:'Belemar',external_code:'1.152.599',title:'Revisão contratual',status:'waiting_internal',priority:'medium',contact:'Samuel',next_action:'Cobrar revisão',next_action_at:new Date().toISOString()},
 {id:'5',client_name:'Leandro Cavazzani Ltda',external_code:'1.144.245',title:'Troca de titularidade',status:'waiting_client',priority:'medium',contact:'Cliente',next_action:'Receber fotos',next_action_at:new Date().toISOString()},
 {id:'6',client_name:'Betânia',external_code:'',title:'Nota de comodato enviada',status:'completed',priority:'medium',contact:'Luana',next_action:'',next_action_at:new Date().toISOString()}
];
const demoClients=[{id:'c1',trade_name:'Márcio Villela',code:'339801',legal_name:'Márcio Villela Comércio de Gases Ltda.',contact_name:'Márcio Villela',phone:'(31) 98765-4321',email:'marcio@villela.com.br',delivery_address:'Av. das Indústrias, 1.520 — Contagem/MG',tank_capacity:1000,avg_consumption_6m:780},{id:'c2',trade_name:'Fernando Bernardo da Silva',code:'449267',contact_name:'Fernando',phone:'',email:''},{id:'c3',trade_name:'Edifício Oscar Niemeyer',code:'1.110.685',contact_name:'',phone:'',email:''}];
const demoContacts=[{id:'r1',name:'Lizane',department:'Financeiro',job_title:'Analista',email:'lizane@empresa.com'},{id:'r2',name:'Sandy',department:'Engenharia',job_title:'Engenheira',email:'sandy@empresa.com'},{id:'r3',name:'Pablo',department:'Operações',job_title:'Analista',email:'pablo@empresa.com'}];
const demoAttachments=[{id:'a1',client_id:'c1',demand_id:'3',original_name:'Lista de peças - Márcio Villela.pdf',kind:'pdf',created_at:new Date().toISOString(),client_name:'Márcio Villela',demand_title:'Lista de peças / OS'},{id:'a2',client_id:'c3',demand_id:'2',original_name:'Projeto aumento de tancagem.pdf',kind:'project',created_at:new Date().toISOString(),client_name:'Edifício Oscar Niemeyer',demand_title:'Aumento de tancagem'}];

function escapeHtml(v=''){return String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));}
function fmtDate(v){return new Intl.DateTimeFormat('pt-BR',{day:'2-digit',month:'2-digit'}).format(new Date(v));}
function todayLong(){return new Intl.DateTimeFormat('pt-BR',{weekday:'long',day:'numeric',month:'long'}).format(new Date());}
function toast(msg){document.body.insertAdjacentHTML('beforeend',`<div class="toast">${escapeHtml(msg)}</div>`);setTimeout(()=>$('.toast')?.remove(),2800)}
async function init(){if(configured){const {data:{session}}=await supabase.auth.getSession();if(session){state.user=session.user;await loadData();return renderApp();}}renderLogin();}
function renderLogin(){document.querySelector('#app').innerHTML=`<main class="login-shell"><section class="login-brand"><div class="brand-lockup"><img class="brand-logo" src="./assets/logo-dds.png" alt="Logo DDS Inovação"><div class="brand-name"><strong>DDS Inovação</strong><small>CENTRAL INTELIGENTE</small></div></div><div><h1>Seu trabalho diário, organizado e conectado.</h1><p>Clientes, demandas, contratos, follow-ups e histórico em uma única central inteligente.</p></div><div><small>Central Inteligente de Trabalho</small><div class="copyright">© ${new Date().getFullYear()} Diogo Soares. Todos os direitos reservados.</div></div></section><section class="login-panel"><form class="login-card" id="login-form"><h2>Entrar</h2><p>Acesse seu ambiente de trabalho.</p>${!configured?'<div class="notice">Modo de demonstração: conecte o Supabase em <b>config.js</b> para ativar login e persistência.</div>':''}<label for="email">E-mail</label><input class="field" id="email" type="email" autocomplete="email" required placeholder="seu@email.com"><label for="password">Senha</label><input class="field" id="password" type="password" autocomplete="current-password" required placeholder="Sua senha"><div id="login-error"></div><button class="btn btn-primary btn-block" type="submit" style="margin-top:20px">Entrar no DDS Inovação</button>${!configured?'<button class="btn btn-outline btn-block" type="button" id="demo" style="margin-top:10px">Ver demonstração</button>':''}</form></section></main>`;$('#login-form').onsubmit=login;$('#demo')?.addEventListener('click',()=>{state.demo=true;state.user={email:'diogo@ddsflow.com'};state.demands=demoDemands;state.clients=demoClients;state.contacts=demoContacts;state.attachments=demoAttachments;renderApp()});}
async function login(e){e.preventDefault();if(!configured)return;const btn=e.submitter;btn.disabled=true;const {data,error}=await supabase.auth.signInWithPassword({email:$('#email').value,password:$('#password').value});btn.disabled=false;if(error)return $('#login-error').innerHTML=`<div class="notice error">${escapeHtml(error.message)}</div>`;state.user=data.user;await loadData();renderApp();}
async function loadData(){const [d,c,p,k,a]=await Promise.all([supabase.from('demands').select('*,clients(trade_name),internal_contacts(name)').order('next_action_at'),supabase.from('clients').select('*').is('archived_at',null).order('trade_name'),supabase.from('internal_contacts').select('*').eq('is_active',true).order('name'),supabase.from('contracts').select('*,clients(trade_name)').order('end_date'),supabase.from('attachments').select('*,clients(trade_name),demands(title)').order('created_at',{ascending:false})]);state.demands=(d.data||[]).map(x=>({...x,client_name:x.clients?.trade_name||'Sem cliente',contact:x.internal_contacts?.name||''}));state.clients=c.data||[];state.contacts=p.data||[];state.contracts=k.data||[];state.attachments=(a.data||[]).map(x=>({...x,client_name:x.clients?.trade_name||'',demand_title:x.demands?.title||''}));}
function nav(){const items=[['day','Meu Dia'],['capture','Registro Rápido'],['demands','Demandas'],['clients','Clientes'],['contracts','Contratos'],['contacts','Responsáveis'],['diary','Diário'],['documents','Documentos'],['dashboard','Dashboard'],['export','Exportar'],['copilot','DDS IA'],['settings','Configurações']];return items.map(([r,l])=>`<button data-route="${r}" class="${state.route===r?'active':''}"><span class="ico">${icons[r]}</span>${l}</button>`).join('')}
function renderApp(){const initials=(state.user?.email||'DS').slice(0,2).toUpperCase();const brand=`<div class="brand-lockup"><img class="brand-logo" src="./assets/logo-dds.png" alt="Logo DDS Inovação"><div class="brand-name"><strong>DDS Inovação</strong><small>CENTRAL INTELIGENTE</small></div></div>`;$('#app').innerHTML=`<div class="shell"><aside class="sidebar">${brand}<nav class="nav" style="margin-top:20px">${nav()}</nav><div class="sidebar-foot">Mais produtividade.<br>Melhores relacionamentos.<div class="copyright">© ${new Date().getFullYear()} Diogo Soares</div></div></aside><main class="main"><header class="topbar"><div class="topbar-brand" style="display:none">${brand}</div><div class="search"><input class="field" id="global-search" placeholder="Buscar clientes, demandas, contratos..."></div><div class="user"><div class="avatar">${initials}</div><span>${escapeHtml(state.user?.email||'Diogo')}</span></div></header><div class="content" id="view"></div></main><button class="fab" id="fab">＋ Registrar</button><nav class="mobile-nav">${[['day','⌂','Meu Dia'],['capture','⊕','Registrar'],['demands','▤','Demandas'],['clients','♙','Clientes'],['settings','•••','Mais']].map(([r,i,l])=>`<button data-route="${r}" class="${state.route===r?'active':''}"><span>${i}</span>${l}</button>`).join('')}</nav></div><div id="modal-root"></div>`;$$('[data-route]').forEach(b=>b.onclick=()=>{state.route=b.dataset.route;renderApp()});$('#fab').onclick=openCapture;$('#global-search')?.addEventListener('keydown',e=>{if(e.key==='Enter'){state.route='demands';state.filter=e.target.value;renderApp()}});renderView();}
function renderView(){const view=$('#view');({day:renderDay,capture:()=>{renderDay();openCapture()},demands:renderDemands,clients:renderClients,contracts:renderContracts,contacts:renderContacts,diary:renderDiary,documents:renderDocuments,dashboard:renderDashboard,export:renderExport,copilot:renderCopilot,settings:renderSettings}[state.route]||renderDay)(view)}
function metric(icon,label,n,small='demandas'){return `<div class="metric"><div class="metric-ico">${icon}</div><div><span>${label}</span><strong>${n}</strong><small>${small}</small></div></div>`}
function renderDay(v){const open=state.demands.filter(d=>d.status!=='completed'),counts={urgent:open.filter(d=>['urgent','high'].includes(d.priority)).length,follow:open.filter(d=>d.status==='need_follow_up').length,waiting:open.filter(d=>['waiting_client','waiting_internal'].includes(d.status)).length,done:state.demands.filter(d=>d.status==='completed').length};v.innerHTML=`<div class="page-head"><div><h1>Bom dia, Diogo</h1><p class="sub">${todayLong()}</p></div><div class="actions"><button class="btn btn-outline" id="close-day">Fechar meu dia</button></div></div><section class="metrics">${metric('!','Prioridade',counts.urgent,'demandas críticas')}${metric('◷','Cobrar hoje',counts.follow)}${metric('♙','Aguardando terceiros',counts.waiting)}${metric('▱','Contratos ≤ 90 dias',state.contracts.filter(c=>days(c.end_date)<=90).length,'para renovação')}${metric('✓','Concluídas hoje',counts.done)}</section><section class="ai-banner"><div><h3>✦ DDS IA</h3><p>Você tem ${counts.follow} demandas para cobrar e ${counts.waiting} aguardando terceiros. Priorize os itens mais antigos.</p></div><div class="actions"><button class="btn btn-primary">Organizar meu dia</button><button class="btn btn-outline">Gerar follow-ups</button></div></section><div class="page-head"><div><h1 style="font-size:25px">Meu Dia</h1><p class="sub">Demandas organizadas por contexto e prioridade.</p></div></div><section class="board">${Object.entries(labels).map(([s,l])=>`<div class="column"><div class="column-head"><span>${l}</span><span>${state.demands.filter(d=>d.status===s).length}</span></div>${state.demands.filter(d=>d.status===s).map(d=>demandCard(d)).join('')||'<div class="empty">Nenhum item</div>'}</div>`).join('')}</section><div class="footer-copy">© ${new Date().getFullYear()} Diogo Soares — Todos os direitos reservados.</div>`;$('#close-day').onclick=openCloseDay;$$('.demand-card').forEach(x=>x.onclick=()=>openDemand(x.dataset.id));}
function demandCard(d){const badge=d.status==='completed'?'green':d.status==='need_follow_up'?'amber':d.status==='waiting_internal'?'purple':'';return `<article class="demand-card" data-id="${d.id}" tabindex="0"><h4>${escapeHtml(d.client_name)}</h4><p>Cód. ${escapeHtml(d.external_code||'—')}</p><p style="color:#233954;font-weight:600">${escapeHtml(d.title)}</p><div class="row" style="margin-top:9px"><span class="badge ${badge}">${escapeHtml(d.contact||labels[d.status])}</span><small>${fmtDate(d.next_action_at||new Date())}</small></div></article>`}
function renderDemands(v){const q=(state.filter||'').toLowerCase();const rows=state.demands.filter(d=>!q||JSON.stringify(d).toLowerCase().includes(q));v.innerHTML=`<div class="page-head"><div><h1>Demandas</h1><p class="sub">Acompanhe, edite ou exclua sem perder o controle.</p></div><button class="btn btn-primary" id="new-demand">＋ Nova demanda</button></div><div class="card table-wrap"><table class="table"><thead><tr><th>Cliente</th><th>Assunto</th><th>Status</th><th>Responsável</th><th>Próxima ação</th><th>Ações</th></tr></thead><tbody>${rows.map(d=>`<tr data-id="${d.id}"><td><b>${escapeHtml(d.client_name)}</b><br><small>${escapeHtml(d.external_code||'')}</small></td><td>${escapeHtml(d.title)}</td><td><span class="badge">${labels[d.status]}</span></td><td>${escapeHtml(d.contact||'—')}</td><td>${escapeHtml(d.next_action||'—')}</td><td class="action-cell"><button class="icon-btn edit" data-edit-demand="${d.id}" title="Editar demanda">✎ Editar</button><button class="icon-btn delete" data-delete-demand="${d.id}" title="Excluir demanda">⌫ Excluir</button></td></tr>`).join('')}</tbody></table></div>`;$('#new-demand').onclick=openCapture;$$('tbody tr').forEach(x=>x.onclick=e=>{if(!e.target.closest('button'))openDemand(x.dataset.id)});$$('[data-edit-demand]').forEach(b=>b.onclick=()=>openDemand(b.dataset.editDemand));$$('[data-delete-demand]').forEach(b=>b.onclick=()=>deleteEntity('demands',b.dataset.deleteDemand,'demanda'));}
function renderClients(v){v.innerHTML=`<div class="page-head"><div><h1>Clientes</h1><p class="sub">Memória completa por cliente, incluindo consumo médio dos últimos seis meses.</p></div><button class="btn btn-primary" id="new-client">＋ Novo cliente</button></div><div class="card table-wrap"><table class="table"><thead><tr><th>Cliente</th><th>Código</th><th>Responsável</th><th>Consumo médio 6 meses</th><th>Contato</th><th>Ações</th></tr></thead><tbody>${state.clients.map(c=>`<tr data-id="${c.id}"><td><b>${escapeHtml(c.trade_name)}</b><br><small>${escapeHtml(c.legal_name||'')}</small></td><td>${escapeHtml(c.code||'—')}</td><td>${escapeHtml(c.contact_name||'—')}</td><td><b>${c.avg_consumption_6m??'—'}</b> ${escapeHtml(c.consumption_unit||'kg')}</td><td>${escapeHtml(c.phone||c.email||'—')}</td><td class="action-cell"><button class="icon-btn edit" data-edit-client="${c.id}">✎ Editar</button><button class="icon-btn delete" data-delete-client="${c.id}">⌫ Excluir</button></td></tr>`).join('')}</tbody></table></div>`;$('#new-client').onclick=()=>openClientForm();$$('tbody tr').forEach(x=>x.onclick=e=>{if(!e.target.closest('button'))openClient(x.dataset.id)});$$('[data-edit-client]').forEach(b=>b.onclick=()=>openClientForm(b.dataset.editClient));$$('[data-delete-client]').forEach(b=>b.onclick=()=>deleteEntity('clients',b.dataset.deleteClient,'cliente'));}
function days(date){return date?Math.ceil((new Date(date)-new Date())/86400000):9999}
function renderContracts(v){v.innerHTML=`<div class="page-head"><div><h1>Radar de contratos</h1><p class="sub">Antecipe renovações e evite urgências.</p></div></div><div class="metrics">${metric('●','Saudável',state.contracts.filter(c=>days(c.end_date)>150).length,'+150 dias')}${metric('●','Preventivo',state.contracts.filter(c=>days(c.end_date)<=150&&days(c.end_date)>90).length,'91–150 dias')}${metric('●','Renovação',state.contracts.filter(c=>days(c.end_date)<=90&&days(c.end_date)>30).length,'31–90 dias')}${metric('●','Crítico',state.contracts.filter(c=>days(c.end_date)<=30&&days(c.end_date)>=0).length,'0–30 dias')}</div><div class="card table-wrap"><table class="table"><thead><tr><th>Cliente</th><th>Contrato</th><th>Início</th><th>Fim</th><th>Tempo restante</th></tr></thead><tbody>${state.contracts.map(c=>`<tr><td><b>${escapeHtml(c.clients?.trade_name||'Cliente')}</b></td><td>${escapeHtml(c.contract_number||'—')}</td><td>${fmtDate(c.start_date)}</td><td>${fmtDate(c.end_date)}</td><td><span class="badge ${days(c.end_date)<=30?'red':days(c.end_date)<=90?'amber':'green'}">${days(c.end_date)} dias</span></td></tr>`).join('')||'<tr><td colspan="5" class="empty">Cadastre contratos na ficha do cliente.</td></tr>'}</tbody></table></div>`}
function renderContacts(v){v.innerHTML=`<div class="page-head"><div><h1>Responsáveis internos</h1><p class="sub">Veja e gerencie tudo que depende de cada pessoa ou área.</p></div><button class="btn btn-primary" id="new-contact">＋ Novo responsável</button></div><div class="grid-2">${state.contacts.map(c=>`<div class="card"><div class="row"><div><h3 style="margin-bottom:5px">${escapeHtml(c.name)}</h3><p class="sub">${escapeHtml(c.department||'Área interna')} · ${escapeHtml(c.job_title||'')}</p></div><div class="action-cell"><button class="icon-btn edit" data-edit-contact="${c.id}">✎</button><button class="icon-btn delete" data-delete-contact="${c.id}">⌫</button></div></div><p>${escapeHtml(c.email||'Sem e-mail cadastrado')}</p><p><b>${state.demands.filter(d=>d.contact===c.name&&d.status!=='completed').length}</b> demandas aguardando</p><button class="btn btn-outline">Gerar e-mail consolidado</button></div>`).join('')||'<div class="card empty">Nenhum responsável cadastrado.</div>'}</div>`;$('#new-contact').onclick=()=>openContactForm();$$('[data-edit-contact]').forEach(b=>b.onclick=()=>openContactForm(b.dataset.editContact));$$('[data-delete-contact]').forEach(b=>b.onclick=()=>deleteEntity('internal_contacts',b.dataset.deleteContact,'responsável'));}
function renderDiary(v){v.innerHTML=`<div class="page-head"><div><h1>Diário</h1><p class="sub">Tudo que aconteceu, por data.</p></div></div><div class="card"><h2>${todayLong()}</h2><div class="metrics">${metric('▤','Registros',state.demands.length,'no dia')}${metric('✓','Concluídos',state.demands.filter(d=>d.status==='completed').length)}${metric('→','Pendentes',state.demands.filter(d=>d.status!=='completed').length,'para o próximo dia')}</div>${state.demands.map(d=>`<div class="row" style="padding:11px 0;border-bottom:1px solid var(--line)"><span>${d.status==='completed'?'✅':'⏳'} <b>${escapeHtml(d.client_name)}</b> — ${escapeHtml(d.title)}</span><span class="badge">${labels[d.status]}</span></div>`).join('')}</div>`}
function renderDocuments(v){v.innerHTML=`<div class="page-head"><div><h1>Documentos por cliente e demanda</h1><p class="sub">Todo documento deve estar vinculado ao cliente e, quando aplicável, à demanda correspondente.</p></div><button class="btn btn-primary" id="new-document">＋ Anexar documento</button></div><div class="card">${state.attachments.map(a=>`<div class="document-item"><div class="doc-icon">${a.kind==='pdf'?'PDF':'DOC'}</div><div><b>${escapeHtml(a.original_name)}</b><br><small>Cliente: ${escapeHtml(a.client_name||'—')} · Demanda: ${escapeHtml(a.demand_title||'—')}</small></div><div class="action-cell"><button class="icon-btn edit">✎ Editar vínculo</button><button class="icon-btn delete" data-delete-document="${a.id}">⌫ Excluir</button></div></div>`).join('')||'<div class="empty">Nenhum documento anexado ainda.</div>'}</div>`;$('#new-document').onclick=()=>openDocumentForm();$$('[data-delete-document]').forEach(b=>b.onclick=()=>deleteEntity('attachments',b.dataset.deleteDocument,'documento'));}
function renderDashboard(v){const done=state.demands.filter(d=>d.status==='completed').length,total=state.demands.length,rate=total?Math.round(done/total*100):0;v.innerHTML=`<div class="page-head"><div><h1>Dashboard</h1><p class="sub">Evolução diária, semanal e mensal.</p></div><div class="actions"><button class="btn btn-primary">Diário</button><button class="btn btn-outline">Semanal</button><button class="btn btn-outline">Mensal</button></div></div><div class="metrics">${metric('▤','Recebidas',total)}${metric('✓','Concluídas',done)}${metric('○','Abertas',total-done)}${metric('%','Taxa de resolução',rate,'%')}</div><div class="grid-2"><div class="card"><h3>Onde estão as pendências?</h3>${['waiting_internal','waiting_client','need_follow_up','in_progress'].map(s=>`<div class="row" style="padding:10px 0;border-bottom:1px solid var(--line)"><span>${labels[s]}</span><b>${state.demands.filter(d=>d.status===s).length}</b></div>`).join('')}</div><div class="card"><h3>Quem estou aguardando?</h3>${[...new Set(state.demands.map(d=>d.contact).filter(Boolean))].map(n=>`<div class="row" style="padding:10px 0;border-bottom:1px solid var(--line)"><span>${escapeHtml(n)}</span><b>${state.demands.filter(d=>d.contact===n&&d.status!=='completed').length}</b></div>`).join('')}</div></div>`}
function renderExport(v){v.innerHTML=`<div class="page-head"><div><h1>Exportar dados</h1><p class="sub">Gere relatórios para Excel ou PDF usando filtros de período e conteúdo.</p></div></div><div class="card" style="margin-bottom:18px"><div class="form-grid"><div><label>Período</label><select class="field" id="export-period"><option value="all">Todos os registros</option><option value="today">Hoje</option><option value="week">Esta semana</option><option value="month">Este mês</option></select></div><div><label>Conteúdo</label><select class="field" id="export-kind"><option value="demands">Demandas</option><option value="clients">Clientes</option><option value="productivity">Produtividade</option><option value="contracts">Contratos</option></select></div></div></div><div class="export-options"><div class="export-card"><div class="export-icon">📊</div><h2>Exportar para Excel</h2><p>Baixa uma planilha compatível com Microsoft Excel contendo os dados e filtros selecionados.</p><button class="btn btn-primary" id="export-excel">Baixar Excel (.xls)</button></div><div class="export-card"><div class="export-icon">📄</div><h2>Exportar para PDF</h2><p>Prepara um relatório limpo e abre a impressão para salvar como PDF.</p><button class="btn btn-outline" id="export-pdf">Gerar PDF</button></div></div><div id="export-preview" class="card" style="margin-top:18px"><h3>Prévia do relatório</h3>${reportTable('demands')}</div><div class="footer-copy">© ${new Date().getFullYear()} Diogo Soares — Todos os direitos reservados.</div>`;$('#export-kind').onchange=e=>$('#export-preview').innerHTML=`<h3>Prévia do relatório</h3>${reportTable(e.target.value)}`;$('#export-excel').onclick=exportExcel;$('#export-pdf').onclick=()=>window.print();}
function renderCopilot(v){v.innerHTML=`<div class="page-head"><div><h1>DDS IA</h1><p class="sub">Pergunte sobre prioridades, clientes, contratos e produtividade.</p></div></div><div class="card" style="max-width:850px"><div id="chat" class="notice" style="background:#edf5ff;color:#29496e">Olá, Diogo. Sou a DDS IA e posso ajudar a organizar seu dia com base nos dados do DDS Inovação.</div><label>Sua pergunta</label><textarea class="field" id="question" placeholder="Ex.: O que tenho de mais urgente?"></textarea><button class="btn btn-primary" id="ask" style="margin-top:12px">✦ Perguntar à DDS IA</button></div>`;$('#ask').onclick=askCopilot;}
function renderSettings(v){v.innerHTML=`<div class="page-head"><div><h1>Configurações</h1><p class="sub">Preferências do seu ambiente.</p></div></div><div class="card" style="max-width:700px"><h3>Perfil</h3><div class="form-grid"><div><label>Nome</label><input class="field" value="Diogo"></div><div><label>Fuso horário</label><input class="field" value="America/Sao_Paulo"></div><div><label>Início do dia</label><input class="field" type="time" value="08:00"></div><div><label>Fim do dia</label><input class="field" type="time" value="17:00"></div></div><button class="btn btn-primary" style="margin-top:18px">Salvar preferências</button>${configured?'<button class="btn btn-ghost" id="logout" style="margin-top:18px">Sair</button>':''}</div>`;$('#logout')?.addEventListener('click',async()=>{await supabase.auth.signOut();location.reload()});}
function modal(content){$('#modal-root').innerHTML=`<div class="modal-back"><section class="modal">${content}</section></div>`;$('.close').onclick=()=>$('#modal-root').innerHTML='';$('.modal-back').onclick=e=>{if(e.target===e.currentTarget)$('#modal-root').innerHTML=''};}
function openCapture(){modal(`<div class="modal-head"><h2>Registro Rápido</h2><button class="close">×</button></div><div class="segmented"><button class="active">✦ Com IA</button><button>Manual</button></div><label>O que aconteceu ou o que você precisa resolver?</label><textarea class="field" id="capture-text" placeholder="Ex.: Fernando 449267 continua sem código novo..."></textarea><button class="btn btn-primary btn-block" id="parse" style="margin-top:12px">✦ Organizar com IA</button><div id="capture-review"></div>`);$('#parse').onclick=parseCapture;}
async function parseCapture(){const text=$('#capture-text').value.trim();if(!text)return toast('Descreva a demanda primeiro.');const review=$('#capture-review');review.innerHTML='<div class="notice">Organizando as informações...</div>';let data;if(configured){const {data:d,error}=await supabase.functions.invoke('ai-assistant',{body:{kind:'quick_capture',text}});if(error)return review.innerHTML=`<div class="notice error">${escapeHtml(error.message)}</div>`;data=d;}else{const code=text.match(/[\d.]{5,}/)?.[0]||'';const clientGuess=(code?text.split(code)[0]:text.split(/[\-:]/)[0]).trim();data={client_name_raw:clientGuess||'Cliente',external_code:code,title:text.slice(0,90),status:/aguard|esper/i.test(text)?'waiting_internal':'new',priority:'high',dependency:'internal_contact',internal_contact_name:/lizane/i.test(text)?'Lizane':'',next_action:'Cobrar retorno',next_action_at:new Date(Date.now()+86400000).toISOString()};}review.innerHTML=`<div class="review"><h3>Revisar e confirmar</h3><dl><dt>Cliente</dt><dd>${escapeHtml(data.client_name_raw||'A definir')}</dd><dt>Código</dt><dd>${escapeHtml(data.external_code||'—')}</dd><dt>Status</dt><dd>${labels[data.status]||'Novo'}</dd><dt>Responsável</dt><dd>${escapeHtml(data.internal_contact_name||'A definir')}</dd><dt>Próxima ação</dt><dd>${escapeHtml(data.next_action||'Definir ação')}</dd><dt>Data</dt><dd>${fmtDate(data.next_action_at||new Date())}</dd></dl><div class="actions" style="margin-top:16px"><button class="btn btn-primary" id="confirm-capture">Confirmar</button><button class="btn btn-outline">Editar</button></div></div>`;$('#confirm-capture').onclick=()=>saveCapture(data,text);}
async function saveCapture(data,source){if(state.demo){state.demands.unshift({id:crypto.randomUUID(),client_name:data.client_name_raw||'Sem cliente',external_code:data.external_code,title:data.title,status:data.status||'new',priority:data.priority||'medium',contact:data.internal_contact_name||'',next_action:data.next_action,next_action_at:data.next_action_at});$('#modal-root').innerHTML='';state.route='day';renderApp();return toast('Demanda registrada.');}const chosenStatus=data.status||'new';const payload={title:data.title||source.slice(0,120),description:source,external_code:data.external_code||null,status:chosenStatus,priority:data.priority||'medium',dependency:data.dependency==='internal_contact'?'other':(data.dependency||'self'),next_action:data.next_action||'Revisar demanda',next_action_at:data.next_action_at||new Date().toISOString(),completed_at:chosenStatus==='completed'?new Date().toISOString():null,source_text:source,origin:'ai',user_id:state.user.id};const {error}=await supabase.from('demands').insert(payload);if(error)return toast(error.message);await loadData();$('#modal-root').innerHTML='';state.route='day';renderApp();toast('Demanda registrada.');}
function openDemand(id){const d=state.demands.find(x=>String(x.id)===String(id));if(!d)return;const docs=state.attachments.filter(a=>String(a.demand_id)===String(id));modal(`<div class="modal-head"><div><small>${escapeHtml(d.client_name)}</small><h2>${escapeHtml(d.title)}</h2></div><button class="close">×</button></div><div class="tabs"><button class="tab active">Detalhes</button><button class="tab">Follow-ups</button><button class="tab">Documentos (${docs.length})</button><button class="tab">Timeline</button></div><div class="form-grid"><div><label>Status</label><select class="field" id="d-status">${Object.entries(labels).map(([k,l])=>`<option value="${k}" ${d.status===k?'selected':''}>${l}</option>`).join('')}</select></div><div><label>Prioridade</label><select class="field" id="d-priority">${[['urgent','Urgente'],['high','Alta'],['medium','Média'],['low','Baixa']].map(([k,l])=>`<option value="${k}" ${d.priority===k?'selected':''}>${l}</option>`).join('')}</select></div><div><label>Próxima ação</label><input class="field" id="d-next" value="${escapeHtml(d.next_action||'')}"></div><div><label>Data</label><input class="field" id="d-date" type="datetime-local" value="${d.next_action_at?new Date(d.next_action_at).toISOString().slice(0,16):''}"></div></div><label>Descrição</label><textarea class="field" id="d-description">${escapeHtml(d.description||'')}</textarea><div class="card" style="margin-top:16px"><div class="row"><h3 style="margin:0">Documentos desta demanda</h3><button class="btn btn-outline" id="attach-to-demand">＋ Anexar</button></div>${docs.map(a=>`<div class="document-item"><div class="doc-icon">PDF</div><div><b>${escapeHtml(a.original_name)}</b><br><small>${escapeHtml(d.client_name)}</small></div><button class="icon-btn delete" data-delete-document="${a.id}">⌫</button></div>`).join('')||'<p class="sub">Nenhum documento anexado.</p>'}</div><div class="actions" style="margin-top:18px"><button class="btn btn-primary" id="save-demand">Salvar alterações</button><button class="btn btn-success" id="complete-demand">✓ Concluir</button><button class="btn btn-outline">✉ Gerar e-mail</button><button class="btn btn-outline">WhatsApp</button><button class="btn btn-danger" id="delete-demand">Excluir demanda</button></div>`);$('#complete-demand').onclick=()=>completeDemand(d);$('#save-demand').onclick=()=>saveDemand(d);$('#delete-demand').onclick=()=>deleteEntity('demands',d.id,'demanda');$('#attach-to-demand').onclick=()=>openDocumentForm(d.client_id,d.id);$$('[data-delete-document]').forEach(b=>b.onclick=()=>deleteEntity('attachments',b.dataset.deleteDocument,'documento'));}
async function completeDemand(d){if(state.demo){d.status='completed';$('#modal-root').innerHTML='';renderApp();return toast('Demanda concluída e mantida no histórico.')}const {error}=await supabase.from('demands').update({status:'completed',completed_at:new Date().toISOString()}).eq('id',d.id);if(error)return toast(error.message);await loadData();$('#modal-root').innerHTML='';renderApp();toast('Demanda concluída.');}
function openClient(id){const c=state.clients.find(x=>String(x.id)===String(id));if(!c)return;const docs=state.attachments.filter(a=>String(a.client_id)===String(id));modal(`<div class="modal-head"><div><small>Clientes</small><h2>${escapeHtml(c.trade_name)}</h2></div><button class="close">×</button></div><div class="tabs"><button class="tab active">Visão geral</button><button class="tab">Demandas</button><button class="tab">Contratos</button><button class="tab">Documentos (${docs.length})</button><button class="tab">Timeline</button></div><div class="grid-2"><div class="card"><h3>Dados do cliente</h3><p><small>Fantasia</small><br><b>${escapeHtml(c.trade_name)}</b></p><p><small>Razão social</small><br>${escapeHtml(c.legal_name||'—')}</p><p><small>Responsável</small><br>${escapeHtml(c.contact_name||'—')}</p><p><small>Telefone / e-mail</small><br>${escapeHtml(c.phone||'—')} · ${escapeHtml(c.email||'—')}</p><p><small>Endereço de entrega</small><br>${escapeHtml(c.delivery_address||'—')}</p><div class="row"><p>Tancagem<br><b>${c.tank_capacity||'—'} ${escapeHtml(c.tank_capacity_unit||'kg')}</b></p><p>Consumo médio dos últimos 6 meses<br><b>${c.avg_consumption_6m||'—'} ${escapeHtml(c.consumption_unit||'kg')}</b></p></div></div><div class="card"><h3>Contrato atual</h3><div class="progress-wrap"><div class="progress"><strong>62%</strong></div><div><h2>218 dias restantes</h2><span class="badge green">Contrato ativo</span></div></div><hr style="border:0;border-top:1px solid var(--line)"><p>🟡 Alerta preventivo <b style="float:right">150 dias</b></p><p>🔵 Renovação <b style="float:right">90 dias</b></p><p>🔴 Crítico <b style="float:right">30 dias</b></p></div></div><div class="card" style="margin-top:18px"><div class="row"><h3 style="margin:0">Documentos do cliente</h3><button class="btn btn-outline" id="attach-client">＋ Anexar</button></div>${docs.map(a=>`<div class="document-item"><div class="doc-icon">${a.kind==='pdf'?'PDF':'DOC'}</div><div><b>${escapeHtml(a.original_name)}</b><br><small>Demanda: ${escapeHtml(a.demand_title||'Documento geral')}</small></div><button class="icon-btn delete" data-delete-document="${a.id}">⌫</button></div>`).join('')||'<p class="sub">Nenhum documento anexado.</p>'}</div><div class="actions" style="margin-top:18px"><button class="btn btn-primary" id="edit-client">✎ Editar cliente</button><button class="btn btn-outline">＋ Nova demanda</button><button class="btn btn-outline">WhatsApp</button><button class="btn btn-outline">E-mail</button><button class="btn btn-danger" id="delete-client">Excluir cliente</button></div>`);$('#edit-client').onclick=()=>openClientForm(c.id);$('#delete-client').onclick=()=>deleteEntity('clients',c.id,'cliente');$('#attach-client').onclick=()=>openDocumentForm(c.id);$$('[data-delete-document]').forEach(b=>b.onclick=()=>deleteEntity('attachments',b.dataset.deleteDocument,'documento'));}
function openClientForm(id){const c=state.clients.find(x=>String(x.id)===String(id))||{};modal(`<div class="modal-head"><h2>${c.id?'Editar cliente':'Novo cliente'}</h2><button class="close">×</button></div><form id="client-form">${clientForm(c)}</form>`);$('#client-form').onsubmit=e=>saveClient(e,c);$('#delete-client-form')?.addEventListener('click',()=>deleteEntity('clients',c.id,'cliente'));}
function clientForm(c){return `<div class="form-grid"><div><label>Fantasia *</label><input class="field" name="trade_name" required value="${escapeHtml(c.trade_name||'')}"></div><div><label>Código</label><input class="field" name="code" value="${escapeHtml(c.code||'')}"></div><div><label>Razão social</label><input class="field" name="legal_name" value="${escapeHtml(c.legal_name||'')}"></div><div><label>Responsável</label><input class="field" name="contact_name" value="${escapeHtml(c.contact_name||'')}"></div><div><label>Telefone</label><input class="field" name="phone" value="${escapeHtml(c.phone||'')}"></div><div><label>E-mail</label><input class="field" name="email" type="email" value="${escapeHtml(c.email||'')}"></div><div><label>Tancagem (kg)</label><input class="field" name="tank_capacity" type="number" min="0" step="0.001" value="${c.tank_capacity||''}"></div><div><label>Consumo médio dos últimos 6 meses (kg) *</label><input class="field" name="avg_consumption_6m" type="number" min="0" step="0.001" required value="${c.avg_consumption_6m??''}"></div><div><label>Início do contrato</label><input class="field" name="contract_start" type="date"></div><div><label>Fim do contrato</label><input class="field" name="contract_end" type="date"></div></div><label>Endereço de entrega</label><input class="field" name="delivery_address" value="${escapeHtml(c.delivery_address||'')}"><div class="actions" style="margin-top:18px"><button class="btn btn-primary" type="submit">Salvar cliente</button>${c.id?`<button class="btn btn-danger" type="button" id="delete-client-form">Excluir</button>`:''}</div>`}
async function saveClient(e,c){e.preventDefault();const fd=new FormData(e.currentTarget);const payload=Object.fromEntries(fd.entries());payload.tank_capacity=payload.tank_capacity?Number(payload.tank_capacity):null;payload.avg_consumption_6m=Number(payload.avg_consumption_6m);delete payload.contract_start;delete payload.contract_end;if(state.demo){if(c.id)Object.assign(c,payload);else state.clients.push({...payload,id:crypto.randomUUID(),consumption_unit:'kg',tank_capacity_unit:'kg'});$('#modal-root').innerHTML='';renderApp();return toast('Cliente salvo.');}payload.user_id=state.user.id;const query=c.id?supabase.from('clients').update(payload).eq('id',c.id):supabase.from('clients').insert(payload);const {error}=await query;if(error)return toast(error.message);await loadData();$('#modal-root').innerHTML='';renderApp();toast('Cliente salvo.');}
function openContactForm(id){const c=state.contacts.find(x=>String(x.id)===String(id))||{};modal(`<div class="modal-head"><h2>${c.id?'Editar responsável':'Novo responsável'}</h2><button class="close">×</button></div><form id="contact-form"><div class="form-grid"><div><label>Nome *</label><input class="field" name="name" required value="${escapeHtml(c.name||'')}"></div><div><label>Setor</label><input class="field" name="department" value="${escapeHtml(c.department||'')}"></div><div><label>Cargo</label><input class="field" name="job_title" value="${escapeHtml(c.job_title||'')}"></div><div><label>E-mail</label><input class="field" name="email" type="email" value="${escapeHtml(c.email||'')}"></div><div><label>Telefone</label><input class="field" name="phone" value="${escapeHtml(c.phone||'')}"></div><div><label>WhatsApp</label><input class="field" name="whatsapp" value="${escapeHtml(c.whatsapp||'')}"></div></div><label>Observações</label><textarea class="field" name="notes">${escapeHtml(c.notes||'')}</textarea><div class="actions" style="margin-top:18px"><button class="btn btn-primary" type="submit">Salvar responsável</button>${c.id?'<button class="btn btn-danger" type="button" id="delete-contact-form">Excluir</button>':''}</div></form>`);$('#contact-form').onsubmit=e=>saveContact(e,c);$('#delete-contact-form')?.addEventListener('click',()=>deleteEntity('internal_contacts',c.id,'responsável'));}
async function saveContact(e,c){e.preventDefault();const payload=Object.fromEntries(new FormData(e.currentTarget).entries());if(state.demo){if(c.id)Object.assign(c,payload);else state.contacts.push({...payload,id:crypto.randomUUID()});$('#modal-root').innerHTML='';renderApp();return toast('Responsável salvo.');}payload.user_id=state.user.id;const query=c.id?supabase.from('internal_contacts').update(payload).eq('id',c.id):supabase.from('internal_contacts').insert(payload);const {error}=await query;if(error)return toast(error.message);await loadData();$('#modal-root').innerHTML='';renderApp();toast('Responsável salvo.');}
function openDocumentForm(clientId='',demandId=''){modal(`<div class="modal-head"><h2>Anexar documento</h2><button class="close">×</button></div><form id="document-form"><label>Cliente *</label><select class="field" name="client_id" required>${state.clients.map(c=>`<option value="${c.id}" ${String(c.id)===String(clientId)?'selected':''}>${escapeHtml(c.trade_name)}</option>`).join('')}</select><label>Demanda</label><select class="field" name="demand_id"><option value="">Documento geral do cliente</option>${state.demands.map(d=>`<option value="${d.id}" ${String(d.id)===String(demandId)?'selected':''}>${escapeHtml(d.client_name)} — ${escapeHtml(d.title)}</option>`).join('')}</select><label>Tipo</label><select class="field" name="kind"><option value="pdf">PDF</option><option value="invoice">Nota fiscal</option><option value="contract">Contrato</option><option value="project">Projeto</option><option value="spreadsheet">Planilha</option><option value="other">Outro</option></select><label>Arquivo *</label><input class="field" type="file" name="file" required accept=".pdf,.png,.jpg,.jpeg,.webp,.docx,.xlsx,.csv"><p class="notice">O arquivo ficará privado e vinculado ao cliente e à demanda selecionada.</p><button class="btn btn-primary" type="submit">Anexar documento</button></form>`);$('#document-form').onsubmit=saveDocument;}
async function saveDocument(e){e.preventDefault();const fd=new FormData(e.currentTarget),file=fd.get('file'),clientId=fd.get('client_id'),demandId=fd.get('demand_id')||null,client=state.clients.find(c=>String(c.id)===String(clientId)),demand=state.demands.find(d=>String(d.id)===String(demandId));if(state.demo){state.attachments.unshift({id:crypto.randomUUID(),client_id:clientId,demand_id:demandId,original_name:file.name,kind:fd.get('kind'),created_at:new Date().toISOString(),client_name:client?.trade_name||'',demand_title:demand?.title||''});$('#modal-root').innerHTML='';state.route='documents';renderApp();return toast('Documento vinculado ao cliente e à demanda.');}toast('Upload seguro será ativado após conectar o Supabase Storage.');}
async function saveDemand(d){const payload={status:$('#d-status').value,priority:$('#d-priority').value,next_action:$('#d-next').value,next_action_at:$('#d-date').value?new Date($('#d-date').value).toISOString():d.next_action_at,description:$('#d-description').value};if(payload.status==='completed')payload.completed_at=d.completed_at||new Date().toISOString();if(state.demo){Object.assign(d,payload);$('#modal-root').innerHTML='';renderApp();return toast('Demanda atualizada.');}const {error}=await supabase.from('demands').update(payload).eq('id',d.id);if(error)return toast(error.message);await loadData();$('#modal-root').innerHTML='';renderApp();toast('Demanda atualizada.');}
async function deleteEntity(table,id,label){if(!confirm(`Excluir este ${label}? Esta ação não pode ser desfeita.`))return;if(state.demo){const map={demands:'demands',clients:'clients',internal_contacts:'contacts',attachments:'attachments'},key=map[table];state[key]=state[key].filter(x=>String(x.id)!==String(id));$('#modal-root').innerHTML='';renderApp();return toast(`${label[0].toUpperCase()+label.slice(1)} excluído.`);}const {error}=await supabase.from(table).delete().eq('id',id);if(error)return toast(error.message);await loadData();$('#modal-root').innerHTML='';renderApp();toast(`${label[0].toUpperCase()+label.slice(1)} excluído.`);}
function reportRows(kind){if(kind==='clients')return state.clients.map(c=>['Cliente',c.trade_name,c.code||'',c.contact_name||'',c.avg_consumption_6m??'',c.consumption_unit||'kg']);if(kind==='contracts')return state.contracts.map(c=>['Contrato',c.clients?.trade_name||'',c.contract_number||'',c.start_date,c.end_date,days(c.end_date)]);if(kind==='productivity')return state.demands.map(d=>['Produtividade',d.client_name,d.title,labels[d.status],d.contact||'',d.next_action||'']);return state.demands.map(d=>['Demanda',d.client_name,d.external_code||'',d.title,labels[d.status],d.next_action||'']);}
function reportTable(kind){const headers=kind==='clients'?['Tipo','Cliente','Código','Responsável','Consumo médio 6m','Unidade']:kind==='contracts'?['Tipo','Cliente','Contrato','Início','Fim','Dias restantes']:['Tipo','Cliente','Código/Assunto','Descrição/Status','Status/Responsável','Próxima ação'];return `<div class="table-wrap"><table class="table"><thead><tr>${headers.map(h=>`<th>${h}</th>`).join('')}</tr></thead><tbody>${reportRows(kind).map(r=>`<tr>${r.map(c=>`<td>${escapeHtml(c??'')}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`;}
function exportExcel(){const kind=$('#export-kind').value,rows=reportRows(kind),headers=kind==='clients'?['Tipo','Cliente','Código','Responsável','Consumo médio 6 meses','Unidade']:kind==='contracts'?['Tipo','Cliente','Contrato','Início','Fim','Dias restantes']:['Tipo','Cliente','Código/Assunto','Descrição/Status','Status/Responsável','Próxima ação'];const xml=`<?xml version="1.0"?><Workbook xmlns="urn:schemas-microsoft-com:office:spreadsheet" xmlns:ss="urn:schemas-microsoft-com:office:spreadsheet"><Worksheet ss:Name="DDS Inovação"><Table>${[headers,...rows].map(r=>`<Row>${r.map(c=>`<Cell><Data ss:Type="String">${escapeHtml(c??'')}</Data></Cell>`).join('')}</Row>`).join('')}</Table></Worksheet></Workbook>`;const blob=new Blob([xml],{type:'application/vnd.ms-excel'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`dds-inovacao-${kind}-${new Date().toISOString().slice(0,10)}.xls`;a.click();URL.revokeObjectURL(url);toast('Relatório Excel gerado.');}
function openCloseDay(){const pending=state.demands.filter(d=>d.status!=='completed');modal(`<div class="modal-head"><h2>Fechar meu dia</h2><button class="close">×</button></div><div class="metrics" style="grid-template-columns:1fr 1fr;margin-top:18px">${metric('✓','Concluídas',state.demands.length-pending.length)}${metric('◷','Pendentes',pending.length)}</div><h3>Demandas pendentes</h3>${pending.map(d=>`<div class="demand-card"><div class="row"><b>${escapeHtml(d.client_name)}</b><span class="badge">${labels[d.status]}</span></div><p>${escapeHtml(d.title)}</p><div class="actions"><button class="btn btn-outline">Amanhã</button><button class="btn btn-ghost">Reprogramar</button><button class="btn btn-success">Concluir</button></div></div>`).join('')}<div class="review"><h3>✦ Resumo da IA</h3><p>Hoje você concluiu ${state.demands.length-pending.length} demandas. Permanecem ${pending.length} pendências, priorizadas para o próximo dia.</p></div><button class="btn btn-primary btn-block" id="finish-day" style="margin-top:16px">Finalizar dia</button>`);$('#finish-day').onclick=()=>{$('#modal-root').innerHTML='';toast('Dia finalizado. As pendências estarão em Meu Dia amanhã.')};}
async function askCopilot(){const q=$('#question').value.trim();if(!q)return;if(!configured){$('#chat').innerHTML=`<b>DDS IA:</b> Você tem ${state.demands.filter(d=>d.status==='need_follow_up').length} cobranças e ${state.demands.filter(d=>d.status==='waiting_internal').length} itens aguardando área interna. O caso Fernando deve ser priorizado.`;return}$('#chat').textContent='A DDS IA está consultando seus dados...';const {data,error}=await supabase.functions.invoke('ai-assistant',{body:{kind:'copilot_query',question:q}});$('#chat').textContent=error?error.message:data.answer;}
init();
